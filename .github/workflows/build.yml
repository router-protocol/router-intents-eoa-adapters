name: Build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
      - name: Install yarn
        run: npm install -g yarn
      - name: Setup test env
        run: |
          touch .env
          echo REPORT_GAS=true >> .env
          echo ENV=testnet >> .env
          echo PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 >> .env
          echo OWNER=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 >> .env
          echo POLYGON_URL=https://polygon.llamarpc.com >> .env
          echo FANTOM_URL=https://rpc2.fantom.network >> .env
          echo ETH_MAINNET_URL=https://eth.llamarpc.com >> .env
          echo GOERLI_URL=https://rpc.ankr.com/eth_goerli >> .env
          echo HOLESKY_URL=https://ethereum-holesky.publicnode.com >> .env
          echo POLYGON_MUMBAI_URL=https://polygon-mumbai-bor.publicnode.com >> .env				
          echo POLYGONSCAN_API_KEY=43A14UI3HB6DHBGGE3JRSZB4J9V3P1S9SE >> .env
          echo AVALANCHE_API_KEY=QAE2JD7XIBCYB6Z6GSKNJIHKZ8XGVYM8AI >> .env
          echo ETHERSCAN_API_KEY=FF9TZXKT2JWZ68M2EJH1FGCX13IB7ZKPUZ >> .env
          cat .env
      - name: Installing dependencies
        run: yarn install --frozen-lockfile
      - name: Test
        run: npm run test
